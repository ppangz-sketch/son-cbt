# 📘 son-cbt 프로젝트 완전 가이드

> 마지막 업데이트: 2026-05-08
> 작업자: 지선님 + DeepSeek V4 Pro (OpenRouter)

---

## 📂 프로젝트 구조

```
son-cbt/
├── index.html              ← 메인 CBT 앱 (HTML+CSS+JS 통합, 약 5,800줄)
├── data_academy.js         ← 학원 모의고사 문제은행 (125문항)
├── README.md               ← 프로젝트 소개
├── robots.txt              ← 크롤러 차단
├── copilot_handoff.md      ← Copilot ↔ OpenRouter 수동 핸드오프 브릿지
├── .ai_config.json         ← AI 도구 설정 (비용 한도, 라우팅 규칙)
├── .ai_handoff.md          ← AI 핸드오프 자동 생성 파일
├── .ai_state.json          ← 현재 작업 상태 추적
├── .ai_cost_log.jsonl      ← AI 비용 로그
├── .gitignore              ← Git 제외 파일 목록
├── 실제기출문제/            ← 기출문제 PDF/이미지 폴더
└── .vscode/
    ├── tasks.json           ← VS Code 태스크 (클릭 한 번 실행)
    └── scripts/
        ├── ai_router.py           ← 🚀 AI 작업 자동 라우터 (메인)
        ├── handoff.py             ← 간편 핸드오프 스크립트
        ├── install_global_router.py ← 글로벌 설치 스크립트
        ├── auto_check.py          ← 포맷 → 검증 → 변경확인 자동화
        ├── format_html.py         ← HTML/CSS/JS 포맷 정리
        ├── validate_html.py       ← HTML 기본 검증
        └── git_commit_push.py     ← Git 커밋 & 푸시 (대화형)
```

---

## 🎮 VS Code 태스크 (Ctrl+Shift+P → Tasks: Run Task)

### 📋 기본 작업

| 태스크명                                  | 설명                                                  |
| ----------------------------------------- | ----------------------------------------------------- |
| `✅ 전체 자동화 (포맷 → 검증 → 변경확인)` | 코드 포맷 정리 후 검증, Git 변경사항 확인까지 한 번에 |
| `🧹 HTML/CSS/JS 포맷 정리`                | 들여쓰기, 공백 등 코드 스타일 정리                    |
| `🔍 HTML 기본 검증`                       | HTML 태그 짝 맞춤, 구조 오류 검사                     |
| `📦 Git 변경사항 확인`                    | `git diff --stat` 실행                                |
| `🚀 Git 커밋 & 푸시 (대화형)`             | 커밋 메시지 입력 후 자동 푸시                         |
| `⚡ Git 원클릭 커밋 + 동기화`             | 날짜+파일명 자동 메시지로 add → commit → pull/rebase → push |
| `🚀 전체 자동화 + 원클릭 커밋·동기화`    | 포맷/검증/변경확인 후 자동 메시지 commit + sync까지 진행 |

### 하단 버튼 + 저장 자동 실행

- 추천 확장: `Gruntfuggly.triggertaskonsave`, `spencerwmiles.vscode-task-buttons`
- `index.html` 저장 시 자동으로 `✅ 전체 자동화 (포맷 → 검증 → 변경확인)` 실행
- VS Code 하단 상태바에 `저장 검사`, `전체 자동화+동기화` 버튼 표시
- Cursor 에이전트가 파일을 수정한 뒤에도 프로젝트 훅으로 자동 점검 실행

### 🤖 AI 작업

| 태스크명                        | 설명                               |
| ------------------------------- | ---------------------------------- |
| `🤖 AI 작업 시작 (자동 라우팅)` | 작업 설명 입력 → 최적 AI 자동 선택 |
| `🔄 AI 핸드오프 → Claude Code`  | 현재 작업을 Claude Code로 넘기기   |
| `🔄 AI 핸드오프 → OpenRouter`   | 현재 작업을 OpenRouter로 넘기기    |
| `🔄 AI 핸드오프 → Copilot`      | 현재 작업을 Copilot으로 넘기기     |
| `📊 AI 작업 상태 확인`          | 현재 AI 작업 상태 + 비용 확인      |
| `💰 AI 비용 현황`               | 오늘/이번주/이번달 AI 비용         |
| `✅ AI 작업 완료`               | 작업 종료 처리                     |

---

## 🤖 AI 도구 사용 전략

### 추천 조합: Copilot + OpenRouter

```
💰 월 예상 비용: $15~20

Copilot ($10/월 정액):
├── 실시간 코드 자동완성
├── 간단한 버그 수정, 함수 추가
├── 작은 리팩토링
└── 무제한 사용

OpenRouter DeepSeek V4 Pro (종량제, 월 $5~10):
├── 전체 코드 감사 (1~2주 1회)
├── 대규모 멀티파일 분석
├── 복잡한 디버깅
└── Copilot이 해결 못 할 때만 사용
```

### 작업 흐름

```
1. Ctrl+Shift+P → "🤖 AI 작업 시작" → 작업 설명 입력
2. Copilot으로 평소 코딩 (자동완성 + 채팅)
3. Copilot이 막히면 → "🔄 AI 핸드오프 → OpenRouter"
4. OpenRouter에 ".ai_handoff.md 읽고 이어서 작업해줘" 라고 말하기
5. 해결되면 → "🔄 AI 핸드오프 → Copilot" → 적용
6. 끝나면 → "✅ AI 작업 완료"
```

---

## 🔧 글로벌 AI 라우터 (모든 프로젝트에서 사용 가능)

### 설치 경로

```
C:\Users\ppangz\.vscode-ai-router\
```

### 터미널 명령어 (PowerShell)

```powershell
# 새 작업 시작
ai-router start "index.html 다크모드 버그 수정"

# 다른 AI로 넘기기
ai-router handoff --to openrouter
ai-router handoff --to copilot
ai-router handoff --to claude_code

# 상태 확인
ai-router status

# 비용 확인
ai-router cost

# 작업 완료
ai-router done
```

---

## 🐛 수정된 버그 목록 (2026-05-08)

|  #  | 버그                                  | 원인                                               | 수정                                                          |
| :-: | ------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------- |
|  1  | 다크모드 점수 박스 그림자 안 뜸       | CSS `[data-theme=\"dark\"]` 따옴표 이스케이프 오류 | `\"` → `"`                                                    |
|  2  | D-day 1일 오차 (5/9 선택 → 5/8 표시)  | `new Date('YYYY-MM-DD')` UTC 해석 버그             | `new Date(y, m-1, d)` 로컬시간 직접 생성                      |
|  3  | 자모칩(ㄱㄴㄷㄹ) 박스 밖으로 튀어나옴 | `inline-flex`에 `overflow` 미설정                  | `max-width:100%`, `overflow:hidden`, `text-overflow:ellipsis` |
|  4  | 자모박스 텍스트 경계 넘침             | 긴 텍스트 `word-break` 미설정                      | `overflow:hidden`, `word-break:break-all`                     |

---

## 📊 CBT 앱 주요 기능

### 메인 화면

- **출제 난이도 선택**: 기본(실전+), 상, 중, 하
- **AI 월 한도 설정**: 예산 초과 알림 (80%/100%)
- **학원 모의고사**: 75문항 (상법25/농재법25/재배학75)
- **제미나이 AI 모의고사**: API로 실시간 문제 생성
- **실제 기출문제**: comcbt 새 탭에서 열기
- **오답 집중학습**: 저장된 오답만 재시험
- **AI 문제은행**: 회차/태그/난이도 필터 + 오답만 재시험
- **합격 전략 컨설팅**: 회차별 진단 + D-day 맞춤 운영안

### 시험 화면

- **타이머**: 90분 (약점 모드는 문항 수 × 1.2분)
- **OMR 답안지**: 전체/미응답 필터, 과목별 점프
- **북마크**: 검토 필요한 문제 표시
- **단축키**: ←→ 이동, 1~4 선택, B 북마크, Del 지우기
- **자동 저장**: 10초마다 진행상황 저장, 페이지 이탈 시 경고
- **일시정지**: 타이머 일시정지/재개

### 결과 화면

- **과목별 점수**: 상법/농재법/재배학 각 점수 + 과락 판정
- **합격 판정**: 평균 60점 + 전 과목 40점 이상
- **오답 상세**: 해설 + AI 집중분석 (함정/핵심개념/암기팁/유사연습문제)
- **회차 분석**: 이전 회차 대비 증감, 합격 가능성
- **맞춤 학습 가이드**: 약점 과목 + 추천 전략

### 합격 전략 컨설팅

- **D-day 설정**: 시험일 선택 → 자동 카운트다운
- **실제 기출 결과 입력**: 과목별 점수 수동 입력
- **종합 진단**: 최근 평균, 추세, 합격 가능성
- **과목별 목표**: 현재 점수 → 목표 점수 + 필요 상승폭
- **회차별 컨설팅**: 매 회차 상세 피드백 + 코치 멘트
- **맞춤 운영안**: D-day 기준 단계별 학습 계획

### 기타

- **다크모드**: ☀️/🌙 토글
- **API 사용 현황**: 키 상태, 호출 횟수, 누적 비용, 모델별 상세
- **오답 선지 패턴**: 반복되는 오답 패턴 분석
- **오류 로그/복구**: AI 생성 오류 기록 + 자동 복구 시스템
- **수정 로그**: 패치 내역 자가진단

---

## 📝 .md 파일 사용법

### .md 파일이란?

**Markdown(.md)** 파일은 텍스트에 서식을 입힐 수 있는 가벼운 문서 형식이야. `.txt`처럼 메모장으로도 열 수 있고, VS Code에서 예쁘게 보여줘.

### VS Code에서 보는 법

```
1. .md 파일 열기
2. Ctrl+Shift+V → 미리보기 (예쁘게 렌더링)
3. Ctrl+K V → 옆에 미리보기 띄우기 (편집하면서 보기)
```

### 기본 문법

```markdown
# 제목1 (가장 큼)

## 제목2

### 제목3

**굵게**
_기울임_

- 순서 없는 목록
- 순서 없는 목록

1. 순서 있는 목록
2. 순서 있는 목록

`코드 한 줄`

| 표  | 만들기 |
| --- | ------ |
| 값1 | 값2    |

> 인용문

--- (구분선)
```

### 이 프로젝트의 .md 파일들

| 파일                 | 용도                                        |
| -------------------- | ------------------------------------------- |
| `README.md`          | 프로젝트 소개 (GitHub 메인 페이지에 표시됨) |
| `copilot_handoff.md` | AI 간 작업 넘기기용 브릿지 파일             |
| `.ai_handoff.md`     | 자동 생성되는 핸드오프 파일                 |

---

## 🚀 빠른 시작 체크리스트

- [ ] VS Code에서 `index.html` 열기
- [ ] `Ctrl+Shift+P` → `Tasks: Run Task` → `✅ 전체 자동화` 실행
- [ ] GitHub Copilot 로그인 확인
- [ ] OpenRouter API 키 준비 (필요시만)
- [ ] `Ctrl+Shift+V`로 이 가이드 미리보기

---

> 💡 **TIP**: 모르는 게 있으면 Copilot 채팅에 "가이드.md 읽고 설명해줘" 라고 말하면 돼!
